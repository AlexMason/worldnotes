---
phase: 01-production-infrastructure
plan: 06
type: execute
wave: 4
depends_on:
  - 1
  - 2
  - 3
  - 4
  - 5
files_modified:
  - vitest.config.ts
  - AGENTS.md
autonomous: true
requirements:
  - INFRA-05
  - INFRA-06

must_haves:
  truths:
    - "Developer runs `npm run test:coverage` and sees 80%+ branch coverage"
    - "CI fails if branch coverage drops below 80%"
    - "Developer reads AGENTS.md and finds test, lint, typecheck, and build commands documented"
    - "package.json scripts are self-documenting with `npm run` listing all available commands"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Updated coverage thresholds: branches 80, functions 80, lines 80, statements 80"
    - path: "AGENTS.md"
      provides: "Documented development commands matching package.json scripts"
  key_links:
    - from: "vitest.config.ts thresholds"
      to: "CI workflow"
      via: "npm run test:coverage"
      pattern: "branches: 80"
    - from: "AGENTS.md"
      to: "package.json scripts"
      via: "documented commands"
      pattern: "npm run (test|typecheck|lint|build)"
---

<objective>
Enforce 80% branch coverage threshold (per D-03 and INFRA-05) and document all development
commands in AGENTS.md (per INFRA-06). This is the final plan — it gates Phase 1 completion
on measurable quality metrics.

Purpose: Coverage thresholds in CI prevent regression in test quality. AGENTS.md documentation
ensures all contributors (human and AI) know exactly how to run tests, lint, typecheck, and build.
Output: Updated vitest.config.ts with 80% thresholds, updated AGENTS.md with command reference.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 1 success criteria: 80%+ branch coverage, AGENTS.md documentation)
@.planning/REQUIREMENTS.md (INFRA-05: coverage thresholds, INFRA-06: commands documented)
@.planning/phases/01-production-infrastructure/01-CONTEXT.md (D-03: v8 coverage, branch primary; D-07: lint errors fail CI)
@.planning/phases/01-production-infrastructure/01-RESEARCH.md (lines 349-356: coverage threshold strategy; lines 605-609: Pitfall 5 — thresholds blocking CI)
@vitest.config.ts (current thresholds at 0 — needs updating to 80)
@AGENTS.md (current content — must add development commands section)
@package.json (current scripts — reference for AGENTS.md documentation)
</context>

<tasks>

<task type="auto">
  <name>task 1: measure current coverage and set 80% thresholds</name>
  <files>vitest.config.ts</files>
  <read_first>
    @vitest.config.ts (current state — thresholds at 0 from Plan 01)
    @.planning/phases/01-production-infrastructure/01-RESEARCH.md (lines 605-609: Pitfall 5 — threshold strategy)
  </read_first>
  <action>
**Step 1: Measure current coverage baseline.**
Run the full test suite with coverage to see where we stand:
```bash
npm run test:coverage
```
Note the branch coverage percentage from the text reporter output. It should be at or above 80% after Plans 02-05 have written tests for all modules.

If branch coverage is below 80%, identify which modules need additional tests:
```bash
npx vitest run --coverage --reporter=verbose
```
The coverage report will show per-file branch coverage. Focus on modules below 80%.

**Step 2: Add missing test cases for low-coverage modules** (if any are below 80%).
Common low-coverage areas:
- `src/editor.ts` — the 489-line monolith has many internal code paths. If editor coverage is low, add additional lifecycle tests for navigation, content loading, and error paths
- `src/cursor.ts` — the `getCaretOffset`, `setCaretOffset`, `findTextPosition` functions may have untested branches. Consider adding `extractText` edge cases (empty elements, deeply nested DOM)
- `src/renderer.ts` — `renderDocument` and the `activeOffset` raw-text fallback may need additional test cases

Add tests to the appropriate existing test files (`src/__tests__/cursor.test.ts`, `src/__tests__/renderer.test.ts`, `src/__tests__/editor.test.ts`) until coverage reaches 80%+.

**Step 3: Update coverage thresholds to 80%.**
Edit `vitest.config.ts` — change the `thresholds` block from 0 to 80:
```typescript
thresholds: {
  branches: 80,
  functions: 80,
  lines: 80,
  statements: 80,
},
```
Also add `'src/plugins/defaults.ts'` to the coverage exclude list if not already excluded — it's a 20-line array constant with no logic:
```typescript
exclude: [
  'src/demo.ts',
  'src/index.ts',
  'src/types.ts',
  'src/plugins/index.ts',
  'src/storage/index.ts',
  'src/plugins/defaults.ts',
],
```

**Step 4: Verify coverage passes with new thresholds.**
```bash
npm run test:coverage
```
This MUST exit 0 — meaning all coverage thresholds are met. If it fails, return to Step 2 and add more tests.

**Step 5: Verify the full CI pipeline locally.**
Simulate what CI will run:
```bash
npm run typecheck && npm run lint && npm run test:coverage && npm run build
```
All four commands must exit 0.
  </action>
  <acceptance_criteria>
    1. `grep -c '"branches": 80' vitest.config.ts` returns 1 (branch threshold set to 80)
    2. `grep -c '"functions": 80' vitest.config.ts` returns 1 (function threshold)
    3. `grep -c '"lines": 80' vitest.config.ts` returns 1 (line threshold)
    4. `grep -c '"statements": 80' vitest.config.ts` returns 1 (statement threshold)
    5. `npm run test:coverage` exits 0 — coverage meets or exceeds all thresholds
    6. `npm run typecheck && npm run lint && npm run test:coverage && npm run build` exits 0 — full CI simulation passes
    7. Coverage text report shows "All coverage thresholds met" or no threshold errors
  </acceptance_criteria>
  <verify>
    <automated>npm run test:coverage</automated>
  </verify>
  <done>Coverage thresholds set to 80% for branches, functions, lines, and statements. Full test suite passes with coverage meeting all thresholds. CI simulation (typecheck → lint → test:coverage → build) passes.</done>
</task>

<task type="auto">
  <name>task 2: update AGENTS.md with development commands</name>
  <files>AGENTS.md</files>
  <read_first>
    @AGENTS.md (full current content — see what exists before modifying)
    @package.json (current scripts block — reference for documented commands)
  </read_first>
  <action>
Update `AGENTS.md` by adding a `## Development Commands` section that documents all available npm scripts. Insert it after the `## Project Overview` section (after line 6) and before the existing `## Editing Guidelines` section.

**The new section to add:**
```markdown
## Development Commands

Run these checks before reporting behavior changes as complete:

| Command | What it does |
|---------|-------------|
| `npm test` | Run all tests with Vitest (single run) |
| `npm run test:watch` | Run tests in watch mode (re-runs on file changes) |
| `npm run test:coverage` | Run tests with v8 coverage report (threshold: 80% branches) |
| `npm run typecheck` | TypeScript type checking (`tsc --noEmit`) |
| `npm run lint` | ESLint static analysis on `src/` (errors fail, warnings allowed) |
| `npm run lint:fix` | Auto-fix ESLint errors where possible |
| `npm run format` | Format all source files with Prettier |
| `npm run format:check` | Check if source files match Prettier config (CI-friendly) |
| `npm run build` | Type-check then bundle library (tsc + vite build) |
| `npm run dev` | Start Vite dev server for the demo page |

**CI Pipeline:** Every push to `main` and every pull request triggers GitHub Actions
which runs: `typecheck → lint → test:coverage → build`. All steps must pass.

**Test Infrastructure:**
- Framework: Vitest 4 with happy-dom browser environment
- Coverage: v8 provider, 80% branch/function/line/statement thresholds
- Test location: `src/__tests__/*.test.ts` (co-located with source modules)
```

**Also update the existing section (lines 9-16) to remove duplication:**
The existing AGENTS.md has a "Run these checks" section. Replace it with a reference to the new table:
```markdown
Run these checks before reporting behavior changes as complete:
- `npm run typecheck && npm test && npm run build`
- See [Development Commands](#development-commands) for the full command reference.
```

**Update the "Repository Notes" section (line 29-30) — add a note about dist/:**
```markdown
This repository intentionally commits `dist/` because the package manifest points consumers
at built files. If Vite build output changes as part of a source change, review and commit
the generated files with the source change.
```

**Also add a note about the missing git remote (Pitfall 6 from RESEARCH.md):**
```markdown
## CI Setup Note

The CI pipeline (`.github/workflows/ci.yml`) is configured but requires a GitHub remote
to execute. Push this repository to GitHub to activate CI. Until then, validate locally:
`npm run typecheck && npm run lint && npm run test:coverage && npm run build`
```

**Do NOT remove or alter the existing content** about project overview, editing guidelines, or repository notes. Only add the Development Commands section and update references.
  </action>
  <acceptance_criteria>
    1. `grep -c "## Development Commands" AGENTS.md` returns 1 (new section added)
    2. `grep -c "npm test.*Vitest" AGENTS.md` returns 1 (test command documented)
    3. `grep -c "npm run typecheck.*tsc" AGENTS.md` returns 1 (typecheck command documented)
    4. `grep -c "npm run lint.*ESLint" AGENTS.md` returns 1 (lint command documented)
    5. `grep -c "npm run build" AGENTS.md` returns 1 (build command documented)
    6. `grep -c "npm run format" AGENTS.md` returns 2 (format and format:check both documented)
    7. `grep -c "happy-dom" AGENTS.md` returns 1 (test infrastructure documented)
    8. `grep -c "80%" AGENTS.md` returns 1 (coverage threshold documented)
    9. `grep -c "CI Pipeline\|GitHub Actions" AGENTS.md` returns 1 (CI documented)
    10. `grep -c "src/__tests__/\*\.test\.ts" AGENTS.md` returns 1 (test location documented)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "## Development Commands" AGENTS.md && grep -c "npm test" AGENTS.md</automated>
  </verify>
  <done>AGENTS.md updated with complete Development Commands section, CI pipeline description, test infrastructure details, and git remote setup note. All requirements from INFRA-06 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coverage report → CI gate | Coverage thresholds determine CI pass/fail. Incorrect thresholds could hide coverage gaps or block legitimate changes |
| AGENTS.md → developer workflow | Documentation accuracy affects developer productivity and CI reliability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Coverage thresholds (too low) | mitigate | Thresholds set to 80% across all metrics. Verified by `npm run test:coverage` exiting 0. If thresholds are lowered, CI review catches the change |
| T-01-06 | Information Disclosure | AGENTS.md contains no secrets | accept | AGENTS.md is publicly visible documentation. No credentials, tokens, or internal URLs are included |
</threat_model>

<verification>
After all tasks complete, run the full Phase 1 verification:
```bash
npm run typecheck           # Zero TypeScript errors
npm run lint                # Zero ESLint errors (warnings OK per D-07)
npm run format:check        # All files match Prettier
npm run test:coverage       # All tests pass + 80%+ coverage on all metrics
npm run build               # Build succeeds, produces dist/ output
npm run dev                 # Dev server starts (Ctrl+C to stop)

# Full CI simulation:
npm run typecheck && npm run lint && npm run test:coverage && npm run build
# MUST exit 0
```
</verification>

<success_criteria>
1. `vitest.config.ts` has coverage thresholds: branches 80, functions 80, lines 80, statements 80
2. `npm run test:coverage` exits 0 with coverage meeting all thresholds
3. Full CI simulation (`typecheck && lint && test:coverage && build`) exits 0
4. `AGENTS.md` includes Development Commands section with all npm scripts documented
5. `AGENTS.md` describes CI pipeline, test infrastructure, and coverage requirements
6. Every Phase 1 success criterion from ROADMAP.md is verifiable:
   - [x] `npm test` runs Vitest in happy-dom ✓
   - [x] `npm run lint` validates all source files ✓
   - [x] CI workflow exists and configured ✓
   - [x] Coverage shows 80%+ branch coverage ✓
   - [x] AGENTS.md documents all commands ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-production-infrastructure/01-06-SUMMARY.md`
</output>
